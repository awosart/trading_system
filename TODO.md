# TODO

Не критичное, но нужное дальше. Одна строка на пункт; обоснования — в CLAUDE.md,
сюда они не дублируются. Пункт с **признаком** делать не раньше, чем признак
наступил: до него это «на будущее», то есть анти-паттерн.

## Данные и импорт

- [ ] Провести `BarFxConverter` в `_run_inputs` — сейчас дефолт
      `SameCurrencyConverter`, и USDJPY/EURJPY отвергают **все** сигналы
      (`fx_rate_unavailable`, замерено 978 на USDJPY H1). Ленты для курса в сторе есть.
- [ ] Перегнать 9 строк `strategies/results.parquet`: их `dataset_hash`
      (`57db281a…`, `739a6edb…`) больше не разрешается — EURUSD H1/H4 переписаны
      импортом. Прогоны в `runs/` читаются, но не воспроизводятся из стора.
- [ ] `--column-map` у `ts data import` — без него CSV без заголовка не прочитать.
- [ ] Реестр для индексов/товаров, если вернутся их ленты: цифры **из спецификации
      брокера**, не подставленные от `NAS100`.

## Мелкие расхождения в коде

- [ ] Докстрока `SameCurrencyConverter` говорит «never a default anywhere», а
      `backtest/spec.py:76` делает его дефолтом. Направление безопасное, но
      утверждение ложно — править докстроку или дефолт, не оставлять оба.
- [ ] `perturb_costs` не трогает `typical_spread_points`, то есть «издержки ×2»
      почти не двигает доминирующую компоненту. Правка меняет контракт, который
      читают `cost_draws` и `robustness.py` — отдельная задача, не побочный эффект.
- [ ] Поставить `py.typed` и разобрать 529 ошибок mypy в `tests/` (93 файла).

## Отложено до названного признака

- [ ] **Признак: второй случай без совпадающего индикатора** → `DerivedOperand`,
      арифметика над полями бара.
- [ ] **Признак: пик использования маржи подойдёт к 95%** (сейчас 63%) →
      повторная проверка маржи на филе, а не только на решении.
- [ ] **Признак: чистка `runs/` станет регулярной** → механизм удаления в
      `ResultsLink` вместо ручного переписывания parquet.
- [ ] **Признак: перегон библиотеки после правки схемы перестанет быть делом
      минут** → разделить «дайджест того, что торгуется» и «как это записано».
- [ ] **Признак: нефть или газ в реестре** → разделить класс `COMMODITY` либо
      завести в профиле маппинг по символам.
- [ ] **Признак: понадобился `QUOTE_AWARE`** → реализовать источник спреда и
      снять двойной счёт полуспреда со `stop_buffer_spread_multiple`.

## Проверить измерением, а не глазами

- [ ] Ни одна пара (стратегия, `exit_ref`) в трёх примерах не проверена
      измерением — комбинаторный тест проверяет совместимость, не качество.
- [ ] `run_parallel` (не `..._to_store`) всё ещё гонит `BacktestResult` целиком
      через трубу; на сотнях прогонов станет узким местом.
